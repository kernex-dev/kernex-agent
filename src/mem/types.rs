//! CLI-side type wrappers and re-exports from `kernex_memory`.
//!
//! Most of the public types referenced by the spec (`SaveEntry`,
//! `MemoryRecord`, `Fact`, `MemoryStats`, `SearchQuery`, `ObservationType`)
//! live in `kernex_memory` and are re-exported here for convenience.
//!
//! Real re-exports land in follow-up commits per `tasks.md` Step 2.2 once the
//! handlers consume them. This scaffold file holds the module slot.
