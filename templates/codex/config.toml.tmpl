# Codex CLI configuration generated by `kx install --agent codex`.
# Sprint F-1 stub. Real `[[mcp_servers]]` block lands in a subsequent
# Phase F commit (F-1.6 file-merge logic).
#
# project = "{{project_name}}"
