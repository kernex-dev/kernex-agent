# Codex CLI configuration managed by `kx install --agent codex`.
# Generated {{install_timestamp}} for project "{{project_name}}".
#
# Edit any non-`[mcp_servers.kernex]` content freely; the kernex MCP
# server entry is upserted in place by every subsequent `kx install`.

[mcp_servers.kernex]
command = "kx"
args = ["serve"]
